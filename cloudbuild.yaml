steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - hello_gcp1
  - --source=.
  - --trigger-http
  - --runtime=python37
  - --allow-unauthenticated
  - --role=roles/iam.serviceAccountUser
  - --service-account=lalit-kiwitech-sa@leasera-200719.iam.gserviceaccount.com
